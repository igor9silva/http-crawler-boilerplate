{
  "name": "crawler-boilerplate",
  "version": "1.0.0",
  "description": "A small HTTP crawler boilerplate written in node.",
  "main": "crawler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^0.20.0"
  }
}
